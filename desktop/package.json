{
  "name": "surplus-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "macOS menu-bar shell for surplus — tray gauges, burn controls, board window.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.4.0"
  }
}
