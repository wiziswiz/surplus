# deps & build
node_modules/
dist/
board/node_modules/
board/dist/
desktop/src-tauri/target/
desktop/src-tauri/gen/
desktop/src-tauri/icons/android/
desktop/src-tauri/icons/ios/

# runtime state — NEVER commit (lives in ~/.surplus, but belt & suspenders)
.surplus/
*.db
*.db-wal
*.db-shm
.usage-cache.json

# secrets — this project reads ~/.claude/.credentials.json at call time only;
# no token may ever be written into this repo
.credentials.json
*.token
.env
.env.*

# logs & scratch
logs/
*.log
.DS_Store
